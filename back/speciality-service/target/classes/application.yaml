spring:
  application:
    name: speciality-service
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      port: 27017
      host: localhost
      database: medhead

server:
  port: 9000